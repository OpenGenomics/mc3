#####
# If generating JSON for multiple samples, replace '</full/file/path>' or 'null' with '<>', then complete reference_prep.sample_list.tab. See
# ./backup_templates/reference_prep.sample_list.template for examples. Be certain to provide a unique identifier under the 'run_uid' header to
# clearly distinguish samples/runs. If *optional* sample-specific values are desired, the appropriate key from this template may be used as
# a column header in reference_prep.sample_list.tab (be careful to maintain tab-delimited format).
####
zipped: {
    class: File
    path: </full/file/path>         # Required file; replace '</full/file/path>' with full path to gzip-compressed reference FASTA to unzip, index, and make dictionary [e.g., /my/path/to/genome.fa.gz]
